{
  "mcpServers": {
    "literature": {
      "type": "stdio",
      "command": "${MAMBA_EXE:-mamba}",
      "args": [
        "run",
        "-n",
        "litai",
        "--cwd",
        "${CLAUDE_PROJECT_DIR:-.}/src",
        "python",
        "-m",
        "mcp_server.server"
      ]
    }
  }
}
